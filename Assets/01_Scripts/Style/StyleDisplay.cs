using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StyleDisplay : MonoBehaviour
{
    // 문체의 UI를 담당하는 스크립트 (ui 적인 조작을 메인으로 사용)

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
